An attempt to build a nice little floor plan browser app, using D3.
It may be useful at some stage to utilise Leaflet simply as a base for measurment / geometry (and potentially for consistent zooming).
